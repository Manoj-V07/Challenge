/* Minimal Prism core stub for basic highlighting */
(function (global) {
  var Prism = {
    languages: {
      javascript: {},
      python: {}
    },
    highlightAllUnder: function (container) {
      var blocks = container.querySelectorAll('code[class*="language-"]');
      blocks.forEach(function (code) {
        // This stub doesn't transform; relies on CSS theme styles
      });
    }
  };
  global.Prism = Prism;
})(typeof window !== 'undefined' ? window : this);